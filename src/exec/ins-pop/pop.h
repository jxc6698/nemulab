#ifndef __POP_H__
#define __POP_H__

make_helper(pop_mv);
make_helper(pop_rv);
make_helper(pop_ds);
make_helper(pop_es);
make_helper(pop_ss);
make_helper(pop_fs);
make_helper(pop_gs);

make_helper(pop_av);

make_helper(pop_fv);

#endif
